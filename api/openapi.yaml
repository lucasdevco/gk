openapi: 3.0.3
info:
  title: GK API
  version: 0.1.0
servers:
  - url: /
paths:
  /api/v1/demo/orders/{scenario}:
    post:
      operationId: runDemoOrder
      summary: Run a bounded development-only observability demonstration
      description: Available only when APP_ENV=development. Runs an order checkout with real PostgreSQL writes in an isolated transaction that is always rolled back. Payment uses a local HTTP simulator; no real charge occurs. At most four concurrent runs per process.
      parameters:
        - name: scenario
          in: path
          required: true
          schema:
            type: string
          description: One of normal, slow-payment, payment-retry, out-of-stock, or payment-declined. Unknown names return 404.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ObservabilityScenarioRequest"
            example: {}
      responses:
        "200":
          description: Completed scenario; intermediate retry failures may have occurred.
          headers:
            X-Trace-Id:
              description: Trace ID when a valid span context exists; does not guarantee export or sampling.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ObservabilityScenarioResult"
        "400":
          $ref: "#/components/responses/Error"
        "404":
          $ref: "#/components/responses/Error"
        "409":
          $ref: "#/components/responses/Error"
        "422":
          $ref: "#/components/responses/Error"
        "503":
          $ref: "#/components/responses/Error"
        "408":
          $ref: "#/components/responses/Error"
        "429":
          $ref: "#/components/responses/Error"
  /api/v1/tasks:
    get:
      operationId: listTasks
      summary: List tasks
      responses:
        "200":
          description: Task list
          content:
            application/json:
              schema:
                type: object
                required: [items]
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/Task"
        "500":
          $ref: "#/components/responses/Error"
    post:
      operationId: createTask
      summary: Create a task
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [title]
              properties:
                title:
                  type: string
                  minLength: 1
                  maxLength: 200
      responses:
        "201":
          description: Created task
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Task"
        "400":
          $ref: "#/components/responses/Error"
        "500":
          $ref: "#/components/responses/Error"
  /api/v1/tasks/{id}:
    patch:
      operationId: updateTask
      summary: Update a task
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [completed]
              properties:
                completed:
                  type: boolean
      responses:
        "200":
          description: Updated task
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Task"
        "400":
          $ref: "#/components/responses/Error"
        "404":
          $ref: "#/components/responses/Error"
        "500":
          $ref: "#/components/responses/Error"
components:
  schemas:
    ObservabilityScenarioRequest:
      type: object
      additionalProperties: false
      properties:
        quantity:
          type: integer
          minimum: 1
          maximum: 10
          default: 1
          description: Demo product quantity; unit price is 1990 cents, initial stock is 10.
        delayMs:
          type: integer
          minimum: 0
          maximum: 3000
          default: 1500
          description: Only accepted by slow-payment; delay in the local payment HTTP service.
        failuresBeforeSuccess:
          type: integer
          minimum: 0
          maximum: 3
          default: 2
          description: Only accepted by payment-retry; failed attempts before eventual success.
    DemoOrder:
      type: object
      required: [id, quantity, totalCents, status, stockRemaining, rolledBack]
      properties:
        id:
          type: string
          description: Temporary demo order ID; cannot be retrieved after rollback.
        quantity:
          type: integer
        totalCents:
          type: integer
        status:
          type: string
          enum: [paid]
        stockRemaining:
          type: integer
        rolledBack:
          type: boolean
          description: Always true on success; neither order nor stock changes persist.
    ObservabilityScenarioResult:
      type: object
      required: [scenario, outcome, attempts, durationMs, order]
      properties:
        order:
          $ref: "#/components/schemas/DemoOrder"
        scenario:
          type: string
        outcome:
          type: string
          enum: [success]
        attempts:
          type: integer
        durationMs:
          type: integer
          format: int64
        traceId:
          type: string
          description: Omitted without a valid span context; not proof of export.
    Task:
      type: object
      required: [id, title, completed, createdAt, updatedAt]
      properties:
        id:
          type: string
        title:
          type: string
        completed:
          type: boolean
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    Error:
      type: object
      required: [error]
      properties:
        error:
          type: object
          required: [code, message]
          properties:
            code:
              type: string
              description: Stable application error code, independent of the HTTP status code.
              example: task_not_found
            details:
              type: object
              additionalProperties: true
              description: Safe machine-readable context; keys use snake_case.
            message:
              type: string
            requestId:
              type: string
  responses:
    Error:
      description: Error response
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
